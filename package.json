{
  "name": "algebra-retake-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "bun run src/cli.ts gen",
    "session": "bun run src/cli.ts session",
    "drill": "bun run src/cli.ts drill",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
